const date = new Date()
const now = Date.now()
const tomorrow = new Date (date.setDate(date.getDate() + 1)).getTime()
console.log(now)
console.log((tomorrow - now))

const datas = [1, 2, 3, 4]
const current = 0
const nextCurrent = 0
datas.forEach(data => {
  if 
 } )
